import { render, screen } from '@testing-library/react'

//🎯 THIS IS EXPECTED NOT TO RUN... DOES NOT MATCH ANY OF THE TESTMATCHER RETGEX IN JEST.CONFIG

//✅ working - does not run
test('tests the tester', () => {
    //-testing setup
});

//✅ working - does not run
test('tests the tester 2', () => {
    expect(1).toBe(1);
});